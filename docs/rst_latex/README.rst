rst_latex
=========

Place files in here that correspond directly to the ``.tex`` files in
the ``latex`` directory. The files that are associated with each other
should have the same name, but with different extensions.
